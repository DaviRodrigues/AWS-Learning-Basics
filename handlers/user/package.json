{
	"name": "user-handler",
	"version": "1.0.0",
	"description": "User creation handler for zero-prod API",
	"main": "dist/index.js",
	"scripts": {
		"build": "esbuild ./index.ts --bundle --platform=node --target=node18 --outfile=dist/index.js",
		"clean": "rm -rf dist",
		"prebuild": "pnpm clean"
	},
	"devDependencies": {
		"@types/aws-lambda": "^8.10.150",
		"@types/node": "^24.0.10",
		"esbuild": "^0.25.6",
		"typescript": "^5.8.3"
	},
	"dependencies": {
		"@aws-sdk/client-dynamodb": "^3.840.0",
		"@aws-sdk/lib-dynamodb": "^3.840.0",
		"uuid": "^11.1.0"
	}
}
